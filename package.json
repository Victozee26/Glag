{
  "name": "udp-burst-proxy",
  "version": "1.0.0",
  "description": "SOCKS5 UDP burst proxy for Free Fire — SocksDroid compatible",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc",
    "run:example": "tsx src/index.ts --port 1080 --hold 2000"
  },
  "devDependencies": {
    "@types/node": "^20.19.41",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3"
  }
}
